# HW3 — Local Wikipedia RAG Assistant
# Fully local stack: Ollama + ChromaDB + SQLite + Streamlit

# HTTP client (Wikipedia API + Ollama REST)
requests>=2.31.0

# Vector store
chromadb>=0.5.0

# UI
streamlit>=1.31.0

# Testing
pytest>=7.0.0
